@app.route("/generate-report", methods=["POST"])
def generate_report():
    data = request.json
    text = data.get("text")

    return {
        "title": "Report",
        "summary": "Generated report",
        "recommendations": []
    }
